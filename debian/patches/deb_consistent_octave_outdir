--- a/PsychSourceGL/Source/linuxmakeitoctave3.m
+++ b/PsychSourceGL/Source/linuxmakeitoctave3.m
@@ -35,6 +35,9 @@ else
     target = 'PsychBasic/Octave3LinuxFiles/';
     target4 = 'PsychBasic/Octave4LinuxFiles/';
 end
+% Debian builds do not support "multiarch" here yet, thus just override with
+% consistent path
+target = 'PsychBasic/Octave3LinuxFiles/';
 
 % Special folder for ARM binaries:
 if ~isempty(strfind(computer, 'arm'))
